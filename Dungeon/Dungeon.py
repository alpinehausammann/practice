#!/usr/bin/python

##Dungeon_Game##
import random
import os


#Initiate game
def clear():
    os.system(if sys="nt", "cls")



#Create Grid for game
class course:
	
	def__init__(self,genCourse,randomLocation):
		self.genCourse = genCourse
		self.randomLocation = randomLocation
		
	def genCourse(self, x, y):
		
		course = []
		for i in range(self.x+1):
			for a in range(self.y+1):
				course.append((i,a))
		return course
#place sprites in random location
	def randomLocation(self, course, spriteCount):
		return rand.sample(self.course,self.spriteCount)

#Set boundries for objects within grid constraints

    

#Create objects for game(player, monster, door)#




#Allow for movement around grid

#restart game


###game

class sprites():
    
		def __init__(self, *args):
			self.*args = *args
        
    
        


